          "name": "hivereader",
          "parameter": {
              "hiveSql": [${hiveSql}],
              "defaultFS": "${defaultFs}",
              "tempDatabase": "${tmpDataBase}",
              "tempDatabasePath": "${tmpPath}",
              "hive_sql_set": "${hiveSetSql}"
           }